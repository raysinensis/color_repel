% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/for_ggplot.R
\name{scale_color_repel}
\alias{scale_color_repel}
\alias{scale_colour_repel}
\alias{scale_fill_repel}
\title{Add a color-repel scale to a ggplot}
\usage{
scale_color_repel(
  nsamp = 50000,
  sim = NULL,
  severity = 0.5,
  verbose = FALSE,
  downsample = 5000,
  polychrome_recolor = FALSE,
  seed = 34,
  col = "colour",
  autoswitch = TRUE,
  layer = 1,
  out_worst = FALSE,
  repel_legend_weight = 0.5,
  label_repel = FALSE,
  encircle = FALSE,
  mascarade = FALSE,
  ...
)

scale_colour_repel(
  nsamp = 50000,
  sim = NULL,
  severity = 0.5,
  verbose = FALSE,
  downsample = 5000,
  polychrome_recolor = FALSE,
  seed = 34,
  col = "colour",
  autoswitch = TRUE,
  layer = 1,
  out_worst = FALSE,
  repel_legend_weight = 0.5,
  label_repel = FALSE,
  encircle = FALSE,
  mascarade = FALSE,
  ...
)

scale_fill_repel(
  nsamp = 50000,
  sim = NULL,
  severity = 0.5,
  verbose = FALSE,
  downsample = 5000,
  polychrome_recolor = FALSE,
  seed = 34,
  col = "fill",
  autoswitch = TRUE,
  layer = 1,
  out_worst = FALSE,
  repel_legend_weight = 0.5,
  label_repel = FALSE,
  encircle = FALSE,
  mascarade = FALSE,
  ...
)
}
\arguments{
\item{nsamp}{how many random sampling color combinations to test, default 50000}

\item{sim}{passing a colorbind simulation function if needed}

\item{severity}{severity of the color vision defect, between 0 and 1}

\item{verbose}{whether to print messages}

\item{downsample}{downsample when too many datapoints are present, or use chull}

\item{polychrome_recolor}{whether to replace the original colors with polychrome creation}

\item{seed}{sampling randomization seed}

\item{col}{colour or fill in ggplot}

\item{autoswitch}{try to switch between colour and fill automatically}

\item{layer}{layer to detect color, defaults to first}

\item{out_worst}{output the worst combination instead of best}

\item{repel_legend_weight}{also repel colors so similar colors are not next to each other in the legend (0 = all based on plot distance, 1 = all based on legend, default 0.5)}

\item{label_repel}{whether to add centroid labels with ggrepel}

\item{encircle}{whether to draw convex hull outlines by cluster}

\item{mascarade}{use mascarade package to outline clusters}

\item{...}{passed to \code{\link[ggplot2:scale_manual]{ggplot2::scale_color_manual()}} or
\code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}}}
}
\value{
object added to a ggplot with \code{+}
}
\description{
\code{scale_color_repel()} can be added to a ggplot with \code{+}. It calculates a
reordered color palette from the plot object, then adds a manual color scale
using those optimized colors.
}
\examples{
a <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(displ, hwy)) +
  ggplot2::geom_point(ggplot2::aes(color = as.factor(cyl)))
b <- a + scale_color_repel()
c <- ggplot2::ggplot(ggplot2::mpg, ggplot2::aes(class, fill = as.factor(cyl))) +
  ggplot2::geom_bar() +
  scale_fill_repel()
}
